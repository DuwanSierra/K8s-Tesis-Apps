apiVersion: v1
kind: Namespace
metadata:
  name: cni-test
  annotations:
    # ArgoCD: desplegar después de metrics (wave -1 = CRDs, wave 0 = stack, wave 2 = tests)
    argocd.argoproj.io/sync-wave: "2"
  labels:
    app.kubernetes.io/part-of: cni-test
    purpose: network-benchmark
